volumes:
  pg_data:
  static:
  media:
  
services:
  db:
    image: postgres:16
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
  redis:
    image: redis:7
    ports:
      - "6379:6379"
  backend:
    build: ./backend/
    env_file: .env
    volumes:
      - ./backend:/app # Чтобы изменения отражались сразу
      - static:/app/staticfiles
      - media:/media/
      - ./data:/data
    depends_on:
      - db
      - redis
  celery_worker:
    build: ./backend/
    env_file: .env
    command: celery -A pitalak_backend worker -l info
    environment:
      - REDIS_HOST=redis
      - DJANGO_SETTINGS_MODULE=pitalak_backend.settings
    depends_on:
      - redis
  gateway:
    build: ./nginx/
    env_file: .env
    volumes:
      - static:/staticfiles
      - media:/media/ 
    depends_on:
      - backend
    ports:
      - 8000:80